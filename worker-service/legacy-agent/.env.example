# =====================================================
# LEGACY AGENT - Configuración de Ejemplo
# =====================================================

# Conexión a SQL Server Legacy (Lince/Zoo Logic)
LEGACY_CONN_STRING=server=localhost;user id=sa;password=Password123!;port=1433;database=LinceIndumentaria

# Blend API URL
BLEND_API_URL=http://localhost:8000/api/v1

# Token de autenticación (obtener haciendo login en Blend)
# Ejecutar: curl -X POST http://localhost:8000/api/v1/auth/login -H "Content-Type: application/json" -d '{"email":"admin@nexuspos.com","password":"admin123"}'
BLEND_API_TOKEN=

# UUID de la tienda en Blend Core
# Obtener de la respuesta del login o consultando GET /admin/tiendas
TIENDA_ID=

# Intervalo de polling (formato: 5s, 1m, 30s, etc.)
POLLING_INTERVAL=5s

# Tamaño de batch para procesamiento
BATCH_SIZE=100
